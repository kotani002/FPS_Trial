using UnityEngine;

public class GamePrefabManager : MonoBehaviour
{
    // Prefabの出し入れを行うManager
    // オブジェクトをScene単位で管理するのではなく、プレハブを出し入れするところから行う
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
